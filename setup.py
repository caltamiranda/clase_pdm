from setuptools import setup, find_packages

setup(
    name='packet',
    version='0.1',
    description='trabajo pdm',
    author='Carlos Altamiranda',
    packages=find_packages(),
    install_requires=[
        # Lista de dependencias requeridas por tu paquete
    ],
)
